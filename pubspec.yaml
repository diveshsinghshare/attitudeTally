name: attitude_app
description: A Flutter app for Attitude Academy login
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.0
  flutter_pdfview: ^1.3.1
  path_provider: ^2.1.2
  dio: ^5.4.0
  webview_flutter: ^4.7.0
  



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
